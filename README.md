Live demo: https://primal-test-js.pages.dev

## Build Project
Copy `.env` to `.env.local` then update Google Maps API Key.

Then run the following command
```
$ yarn build
```
Then distribute `dist` directory
